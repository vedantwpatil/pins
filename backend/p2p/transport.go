package p2p

// Peer is the interface which represents the remote node/remote connection
type Peer interface{}

// Transport is what handles the communication between nodes in the network. This can be of the form (TCP, UDP, websockets ...)
type Transport interface{}
